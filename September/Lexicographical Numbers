class Solution:
    def lexicalOrder(self, n: int) -> List[int]:
        arr = sorted([str(i) for i in range(1,n+1)])
        final=[]
        for i in arr:
            final.append(int(i))
        return final
