class Solution(object):
    def numSteps(self, s):
        a=int(s,2)
        count=0
        while(a!=1):
            if a%2!=0:
                a=a+1
            else:
                a=a/2
            count=count+1
        return count
